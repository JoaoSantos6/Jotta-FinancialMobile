import 'package:flutter/material.dart';

/// Renda — fontes recorrentes e avulsas. Vazia neste ADR; conteúdo chega
/// no ADR-4 (M3).
class IncomePage extends StatelessWidget {
  const IncomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Nada aqui ainda — Renda')));
  }
}
