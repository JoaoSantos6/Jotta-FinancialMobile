import 'package:flutter/material.dart';

/// Home — visão geral. Vazia neste ADR; período, cartão de resumo e
/// distribuição chegam no ADR-3 (M2).
class OverviewPage extends StatelessWidget {
  const OverviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Nada aqui ainda — Home')));
  }
}
